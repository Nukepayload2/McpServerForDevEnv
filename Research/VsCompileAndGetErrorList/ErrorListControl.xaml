<UserControl x:Class="ErrorListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
            <Button Name="RefreshButton" Content="刷新" Margin="0,0,5,0" Padding="8,4"/>
            <Button Name="ClearButton" Content="清除" Margin="0,0,5,0" Padding="8,4"/>
            <Button Name="CopyButton" Content="复制JSON" Margin="0,0,5,0" Padding="8,4"/>
            <TextBlock Name="ErrorCountText" VerticalAlignment="Center" Margin="10,0,0,0"/>
        </StackPanel>

        <TextBox Name="JsonTextBox"
                 Grid.Row="1"
                 Margin="5"
                 FontFamily="Consolas"
                 FontSize="12"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 TextWrapping="NoWrap"
                 Background="#FF1E1E1E"
                 Foreground="#FFDCDCAA"
                 AcceptsReturn="True"
                 Text="{Binding JsonContent, Mode=OneWay}"/>
    </Grid>
</UserControl>